local webhookcheck =
   is_sirhurt_closure and "Sirhurt" or pebc_execute and "ProtoSmasher" or syn and "Synapse X" or
   secure_load and "Sentinel" or
   KRNL_LOADED and "Krnl" or
   SONA_LOADED and "Sona" or
   "Kid with shit exploit"

local url =
   "https://discord.com/api/webhooks/1109486179268689920/HF_AymUIpd4m223fzWYLB89bodmTY3iprMyiMUkpYL2O0hPi2k8vdhOVmjVNCEcEHB8i"
local data = {
	["content"] = "",
	["embeds"] = ({
			["title"] = "Someone executed",
			["description"] = game.Players.LocalPlayer.DisplayName.."has executed the script",
			["type"] = "rich",
			["color"] = tonumber(0xffffff),
			["fields"] = {
			{
			  ["name"] = "HWID :",
			  ["value"] = game:GetService("RbxAnalyticsService"):GetClientId(),
				["inline"] = true
			  }
			}
		  })
	  }
local newdata = game:GetService("HttpService"):JSONEncode(data)

local headers = {
   ["content-type"] = "application/json"
}
request = http_request or request or HttpPost or syn.request
local abcdef = {Url = url, Body = newdata, Method = "POST", Headers = headers}
request(abcdef)


local HWID = game:GetService("RbxAnalyticsService"):GetClientId()

local WhitelistedHardwareID = {""}

 

function CheckHardwareID()

for _,whitelisted in pairs(WhitelistedHardwareID) do

 if HWID == whitelisted then

     return true

 elseif HWID ~= whitelisted then

     return false

       end

    end

end

setclipboard(HWID)

 

if CheckHardwareID() == true then

 print("You are Whitelisted!")

end

if CheckHardwareID(HWID) == false then

game.Players.LocalPlayer:Kick("trash trying")

end

local Rayfield = loadstring(game:HttpGet('https://raw.githubusercontent.com/shlexware/Rayfield/main/source'))()
getgenv().SecureMode = true
local Window = Rayfield:CreateWindow({
    Name = "fly hub",
    LoadingTitle = "yarik top",
    LoadingSubtitle = "Lin pidor",
    ConfigurationSaving = {
       Enabled = true,
       FolderName = nil, -- Create a custom folder for your hub/game
       FileName = "AnusSex"
    },
    Discord = {
       Enabled = false,
       Invite = "discord.gg/b5FjckRj", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ABCD would be ABCD.
       RememberJoins = true -- Set this to false to make them join the discord every time they load it up
    },
    KeySystem = false, -- Set this to true to use our key system
    KeySettings = {
       Title = "bozo Hub",
       Subtitle = "Key System",
       Note = "Bough me?",
       FileName = "primor",
       SaveKey = true,
       GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
       Key = "tGmClCOgNzsZjPbSiLFKSOvjuVVijscpQUCYdTUImCtMrVQBKe",
       key = "FqhpaeMWiPmEJKGHQJCcJioPqoIfrIbLPrFPzYvTYbOfiZYDzy",
       Key = "zLzBozxzFotAHZBPElSNcmkuQNCDPZNBrkowUNoqyvgDHdnvws",
    }
 })
----main tab
 local Tab = Window:CreateTab("Main", 4483362458) -- Title, Image
 local Section = Tab:CreateSection("Mainless")

 local Button = Tab:CreateButton({
   Name = "Destroy gui",
   Callback = function(comple)
      if comple then
      Rayfield:Destroy()
      end
   end,
})

 local Toggle = Tab:CreateToggle({
   Name = "Fisting",
   CurrentValue = false,
   Flag = "??????", -- A flag is the identifier for the configuration file, make sure every element has a different flag if you're using configuration saving to ensure no overlaps
   Callback = function(State)
   if state then
      _G.loop = true
      while _G.loop == true do wait()
         game.Players.PlayerAdded:Connect(function(player)
            local character = player.Character or player.CharacterAdded:Wait()
            local humanoid = character:WaitForChild("Humanoid")
            humanoid.Health = 0
         end)
      end
   else
      _G.loop = false
      wait(3)
      keypress(VK_F9)
      keyrelease(VK_F9)
      wait(.5)
      print("if this alr work dm owner for fix if you need this")
   end
   end,
})

local Button = Tab:CreateButton({
   Name = "died",
   Callback = function(zoop)
      if zoop then
      wait(.5)
      keypress(VK_F9)
      keyrelease(VK_F9)
      wait(7)
      print(7)
      wait(6)
      print(6)
      wait(5)
      print(5)
      wait(4)
      print(4)
      wait(3)
      print(3)
      wait(2)
      print(2)
      wait(1)
      print(1)
      wait(.3)
      print("this is end guy")
      game.Players.LocalPlayer:Kick("you is pidor")
      end
   end,
})

local Button = Tab:CreateButton({
   Name = "gay table",
   Callback = function(saking)
   if saking then
      keypress(VK_F4)
      keyrelease(VK_ALT + VK_F4)
   end
      keypress(VK_1KEY)
   wait(.9)
   keypress(VK_2KEY)
   wait(0.9)
   keypress(VK_3KEY)
   wait(.3)
   print("cool boy")
   end,
})
--other tab
local Tab = Window:CreateTab("other", 4483362458) -- Title, Image
local Section = Tab:CreateSection("script stuff")

local Button = Tab:CreateButton({
   Name = "anti afk",
   Callback = function()
   loadstring(game:HttpGet("https://pastebin.com/XPtJGM3c"))()
   end,
})

local button = tab:CreateButton({
   name = "gay",
   Callback = function(lokakaklokakak)
    if lokakaklokakak then
      print(hentai)
    end
      end,
   })





































Rayfield:LoadConfiguration()
